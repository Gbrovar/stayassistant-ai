⚠️ Nota CTO importante

Este rate limit funciona por IP.

Para el widget es perfecto porque:

1 huésped ≈ 1 IP

Más adelante podremos hacer rate limit por propertyId.

Pero no ahora (evitar overengineering).

-------------------------------------------

🧠 Nivel 2 (futuro) — Embeddings

Cuando escales a muchos hoteles deberás usar:

vector embeddings

Arquitectura:

guest message
     ↓
embedding search
     ↓
retrieve best knowledge
     ↓
AI

Pero esto no es necesario todavía.

Tu sistema puede escalar a miles de hoteles sin embeddings.

----------------------------------------------

7️⃣ SEGURIDAD

Tu backend tiene:

Sistema	Estado
Rate limit	✅
Property isolation	✅
Demo protection	✅
Usage limits	✅

Falta añadir más adelante:

JWT refresh
API abuse protection

Pero para early SaaS está bien.

-----------------------------------------------

5️⃣ Mejora CTO recomendada (muy importante)

Más adelante conviene guardar también el plan en Redis Property para evitar consultar Stripe.

Ejemplo futuro:

property.plan = pro

Así getUsageLimit() será instantáneo.

🚀 Siguiente paso después de esto

Cuando hagas deploy, el siguiente paso es probar el webhook real con Stripe CLI, porque ahora mismo Stripe no está enviando eventos a tu servidor.

El siguiente paso será:

stripe listen
stripe trigger checkout.session.completed

Esto nos permitirá verificar que Redis guarda:

stayassistant:subscription:property_x

--------------------------------------------

🚀 Y después viene algo muy potente

El último gran bloque antes de escalar:

AI Cost Shield System

Esto reduce el coste OpenAI hasta 70-80%.

Incluye:

intent routing
semantic caching
response compression
fallback answers

Es lo que permite escalar a 10k hoteles sin pagar 20k€ en OpenAI.

----------------------------------------------

💡 Mejora UX recomendada

Además, solo deberías mostrar el botón si existe una suscripción real.

Ejemplo:

{subscription.plan !== "free" && (
  <button onClick={openPortal}>
    Open billing portal
  </button>
)}

O mejor aún:

Free plan → mostrar "Upgrade"
Paid plan → mostrar "Manage billing"
🚀 Mejor UX SaaS (recomendado)

Tu sección final debería verse así:

Si FREE
Upgrade your plan
[Upgrade to Pro]
Si PRO/BUSINESS
Manage billing
[Open billing portal]

Esto evita confusión al usuario.

------------------------------------

config.js también está correcto
export const API_URL =
  import.meta.env.VITE_API_URL || "http://localhost:3000"

En producción pondrás en Railway:

VITE_API_URL=https://stayassistantai.com

y todo funcionará.

-------------------------------------
⚠️ Nota importante (muy SaaS)

Cada vez que cambies el dashboard tendrás que hacer:

npm run build

y copiar a:

public/dashboard

En el siguiente paso te enseñaré a automatizar esto, para que Railway lo haga solo.

--------------------------------------------

🧠 Mejora adicional (muy importante)

Ahora mismo cada chat se guarda con TTL 6h:

En tu chat endpoint:

await redis.set(historyKey, JSON.stringify(history), {
  EX: 60 * 60 * 6
})

Eso significa que:

conversation index
≠
conversation history

Resultado:

id existe
pero chat ya expiró

→ lista vacía
→ posibles errores

Te recomiendo cambiar a:

EX: 60 * 60 * 24 * 7

o incluso sin TTL para analytics.

---------------------------------------------------------

RESPUESTA API

Si el usuario ya existe:

{
  "error": "email already registered"
}

El frontend puede mostrar:

"This email is already registered"

----------------------------------------------------


6️⃣ Mejora del sistema de idiomas (recomendado)

Ahora mismo el chat soporta solo:

English
Español
Deutsch

Podemos ampliarlo fácilmente a:

French
Italian
Portuguese
Dutch

y hacerlo 100% automático.

Pero esto implica modificar:

chat.js
promptBuilder.js
AI response cleaner

Es una mejora que recomiendo hacer justo después de arreglar el problema de ubicación.


--------------------------------------------------------

⚠️ DETALLE 2 (FINO PERO IMPORTANTE)
🔁 ESTO SIGUE CONSUMIENDO IA (SIN QUERER)

Aquí:

if (intent === "taxi") { ... }
if (intent === "pharmacy") { ... }
if (intent === "transport") { ... }

✔ bien → no AI

Pero aquí:

// CHECKIN / CHECKOUT
const translation = await openai.chat.completions.create(...)

y

// FAQ translation
const translation = await openai.chat.completions.create(...)
❌ PROBLEMA

👉 ESTO TAMBIÉN USA OPENAI
👉 pero NO incrementa usage

👉 = fuga de coste

💡 SOLUCIÓN PRO (FASE 7.3 PREVIEW)

Tienes 2 opciones:

OPCIÓN A (rápida)

No traducir con AI → devolver en idioma base

OPCIÓN B (pro)

Solo traducir si:

if (currentUsage < limit)

---------------------------------------------------

📌 LO QUE QUEDA PENDIENTE (GUARDAMOS)

❗ eliminar “I'm not sure…” en intents de places

❗ sincronizar AI + tarjetas

❗ mejorar UX fallback

👉 esto será FASE 9 (UX polish)

-------------------------------------------

🔥 FASE 11 — CONVERSION ENGINE

pricing psychology

trial strategy

upgrade triggers

revenue optimization



